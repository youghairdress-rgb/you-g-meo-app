rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // posts/ と stories/ フォルダ: 認証済みユーザーのみ書き込み可、読み取りは公開
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
